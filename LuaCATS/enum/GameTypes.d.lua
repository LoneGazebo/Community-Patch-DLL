--- @meta

--- Binary-only enum (registered by engine executable, not the DLL)
--- @enum GameType
GameTypes = {
	GAME_TYPE_NONE = -1,
	GAME_SINGLE_PLAYER = 0,
	GAME_NETWORK_MULTIPLAYER = 1,
	GAME_HOTSEAT_MULTIPLAYER = 2,
	GAME_EMAIL_MULTIPLAYER = 3,
}
