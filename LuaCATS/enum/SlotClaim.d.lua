--- @meta

--- Binary-only enum (registered by engine executable, not the DLL)
--- @enum SlotClaim
SlotClaim = {
	SLOTCLAIM_UNASSIGNED = 0,
	SLOTCLAIM_RESERVED = 1,
	SLOTCLAIM_ASSIGNED = 2,
	NUM_SLOTCLAIMS = 3,
}
